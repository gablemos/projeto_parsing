package Testes;

import projeto_parsing.LerArquivo;

import java.io.IOException;
import java.util.ArrayList;
import java.util.List;

public class LinhaParsing {

    public static void main(String[] args) throws IOException {
        /*LerArquivo lerArquivo = new LerArquivo();
        String filePath = "C:\\Users\\Focusnetworks\\Documents\\GitHub\\projeto_parsing\\";
        String fileName = "teste.txt";

        List<String> docLido = lerArquivo.getCorpo(lerArquivo.mensagemLida(filePath,fileName));

        List<String[]> parsing = new ArrayList<>();

        for (String linha : docLido){
            parsing.add(linha.split("\t"));
        }

        for (String[] linhaParsing : parsing){
            for (String split : linhaParsing){
                System.out.println(split);
            }
        }*/
    }
}
